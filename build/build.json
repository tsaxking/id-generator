{
    "ignore": [
        // same format as .gitignore
        // "*.css"
        // if files are ignored, build will create ./ignore-list.txt
    ],
    "minify": true,
    "streams": {
    },
    "buildDir": "../static/build/"
}